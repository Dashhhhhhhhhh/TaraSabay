import { useState } from "react";

function RequestResponseForm({ onSubmit }) {
  const [message, setMessage] = useState("");
  const [error, setError] = useState(null);
}

export default RequestResponseForm;
